version: '3.8'
services:
  ubuntu:
    build:
      context: .
      dockerfile: Dockerfile.ubuntu
    volumes:
      - ../..:/app
  almalinux9:
    build:
      context: .
      dockerfile: Dockerfile.almalinux9
    volumes:
      - ../..:/app
  almalinux10:
    build:
      context: .
      dockerfile: Dockerfile.almalinux10
    volumes:
      - ../..:/app
